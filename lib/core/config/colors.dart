import 'dart:ui';

class ConsColors {
  // ============================================================
  // LIGHT BRAND / PRIMARY
  // ============================================================

  /// رنگ اصلی Light
  static const Color greenBg = Color(0xFF3FE2D1);

  /// رنگ اصلی فعال / آیکون / Active
  static const Color green = Color(0xFF17CFCF);

  /// فیروزه‌ای ثانویه
  static const Color blueBg2 = Color(0xFF17CFCF);

  /// فیروزه‌ای روشن
  static const Color blueLight = Color(0xFFE8F4F4);

  /// رنگ روشن‌تر برای Gradient
  static const Color blueBg1 = Color(0xFF88D7C1);

  // ============================================================
  // TEXT - LIGHT
  // ============================================================

  /// متن اصلی
  static const Color blue = Color(0xFF01325B);

  /// متن ثانویه
  static const Color gray = Color(0xFF98AAB9);

  /// متن Disabled
  static const Color grayDisabled = Color(0xFFB8C2C8);

  /// Border غیرفعال
  static const Color borderInactive = Color(0xFFB8C2C8);

  /// Border فعال
  static const Color borderActive = Color(0xFF01325B);

  /// Divider سبز
  static const Color dividerGreen = Color(0xFF3FE2D1);

  /// وضعیت موفق
  static const Color success = Color(0xFF2FBF8F);

  /// هشدار اول
  static const Color orange = Color(0xFFFFB24C);

  /// هشدار دوم
  static const Color orange2 = Color(0xFFFF8A00);

  /// رنگ پس‌زمینه ویژه برای پروفایل
  static const Color yellowBKProfile = Color(0xFFFFF4C9);

  /// خطا
  static const Color error = Color(0xFFEA4335);

  /// سایه
  static const Color shadow = Color(0x33000000);

  // ============================================================
  // LIGHT BACKGROUND
  // ============================================================

  /// پس‌زمینه اصلی
  static const Color lightBackground = Color(0xFFE8F4F4);

  /// Surface
  static const Color lightSurface = Color(0xFFFFFFFF);

  /// Card
  static const Color lightCard = Color(0xFFFFFFFF);

  /// Input
  static const Color lightInput = Color(0xFFE8F4F4);

  // ============================================================
  // DARK BRAND / PRIMARY
  // ============================================================

  /// رنگ اصلی دکمه‌ها
  static const Color darkPrimary = Color(0xFFE31B23);
  // static const Color darkPrimary = Color(0xFF680A10);

  /// رنگ انتخاب شده / Active
  // static const Color darkPrimaryActive = Color(0xFFE31B23);
  static const Color darkPrimaryActive = Color(0xFF680A10);

  /// رنگ دوم Primary
  static const Color darkPrimaryLight = Color(0xFF8E1118);

  // ============================================================
  // DARK APP BAR
  // ============================================================

  /// پس‌زمینه AppBar
  static const Color darkAppBar = Color(0xFF080A09);

  /// رنگ دوم AppBar
  static const Color darkAppBarSecondary = Color(0xFF767978);

  // ============================================================
  // DARK BACKGROUND
  // ============================================================

  /// پس‌زمینه کل صفحات
  static const Color darkBackground = Color(0xFF111513);

  /// Surface
  static const Color darkSurface = Color(0xFF111513);

  /// Card
  static const Color darkCard = Color(0xFF111513);

  /// Input
  static const Color darkInput = Color(0xFF292C2B);

  /// Bottom Navigation
  static const Color darkNavigation = Color(0xFF111513);

  // ============================================================
  // DARK TEXT
  // ============================================================

  /// متن اصلی
  static const Color darkTextPrimary = Color(0xFFFFFFFF);

  /// متن ثانویه
  static const Color darkTextSecondary = Color(0xFF767978);

  /// متن Disabled
  static const Color darkTextDisabled = Color(0xFF707574);

  /// Hint داخل Input
  static const Color darkInputHint = Color(0xFF4E5050);

  /// Divider تاریک
  static const Color darkDivider = Color(0xFF303231);

  /// سایه تاریک
  static const Color darkShadow = Color(0x66000000);

  // ============================================================
  // DARK BORDER
  // ============================================================

  /// Border معمولی
  static const Color darkBorderInactive = Color(0xFF303231);

  /// Border انتخاب شده
  static const Color darkBorderActive = Color(0xFFFFFFFF);

  // ============================================================
  // DARK BACK BUTTON
  // ============================================================

  /// پس‌زمینه دکمه بازگشت
  static const Color darkBackButtonBackground = Color(0xFF261615);

  /// Stroke دکمه بازگشت
  static const Color darkBackButtonStroke = Color(0xFF680A10);
}
